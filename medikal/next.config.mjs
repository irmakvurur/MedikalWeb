/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['example.com','goldmedikal.com'], // Buraya izin verilen alan adını ekleyin
      },
};

export default nextConfig;
